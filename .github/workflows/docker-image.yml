name: Docker Image Smartport
#Ref https://github.com/aws-actions

env:
  REPOSITORY: smartport
  IMAGE: test

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    name: Build Image
    runs-on: ubuntu-latest

    permissions:
      id-token: write # required to use OIDC authentication
      contents: read # required to checkout the code from the repo

    steps:

    - name: Check out code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: arn:aws:iam::557286492042:role/AWS-Github-Actions
        role-duration-seconds: 900 # the ttl of the session, in seconds. Needs to be less than the duration allowed for the role, configured on IAM manager in AWS
        aws-region: us-east-1 # use your region here.
    
    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push docker image to Amazon ECR
      env:
        REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        IMAGE_TAG: $IMAGE_${{ steps.date.outputs.date }} #${{ github.sha }}
#      working-directory: ./$REPOSITORY
      run: |
        echo  $REPOSITORY $PWD ${{ github.workspace }} $(date +'%Y-%m-%d')
#        docker build --file Dockerfile.$IMAGE-t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
#        docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG
